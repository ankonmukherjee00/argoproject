apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mypp.fullname" . }}-test-connection"
  labels:
    {{- include "mypp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mypp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
